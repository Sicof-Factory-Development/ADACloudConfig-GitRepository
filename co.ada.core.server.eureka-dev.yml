#Port
server:
  port: 8761

info:
  component: Discovery Server  

#Eureka Config 
eureka:
  instance:
    hostname: http://eureka-mig.adacsc.co
    #hostname: localhost
  client:
    #telling the server not to register himself in the service registry
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}
      #:${server.port}/eureka
      #defaultZone: http://localhost:8761/eureka, http://localhost:8762/eureka, http://localhost:8763/eureka
  server:
    waitTimeInMsWhenSyncEmpty: 0    #wait time for subsequent sync  

#Peer 1
#spring:
#  profiles: peer1
#eureka:
#  instance:
#    hostname: http://localhost:8761

#Peer 2
#spring:
#  profiles: peer2
#eureka:
#  instance:
#    hostname: http://localhost:8762
    
#Peer 3    
#spring:
#  profiles: peer3
#eureka:
#  instance:
#    hostname: http://localhost:8763   
